# Security claim index

This index uses the evidence vocabulary fixed by
[ADR 0001](adr/0001-phase-1-scope-threat-model-and-tcb.md). Every **Proved** row
names an independently restated theorem in the default-built
`LeanOS.SecurityClaims` contract. Proofs apply to Lean models only; generated C,
the runtime, boot code, QEMU, hardware, timing, concurrency, and covert channels
remain excluded unless a separate refinement claim says otherwise.

“Proved” means accepted by the pinned Lean toolchain under ADR 0001's complete
proof TCB. Declarations using or transitively depending on `native_decide` rely
on `Lean.ofReduceBool`, the Lean compiler and generated native code, the native
evaluator/interpreter, and relevant `@[implemented_by]` definitions; their
decidable computation is not replayed by kernel reduction. The repository-owned
policy check inventories and classifies those source modules without treating
the compiler as verified.

<!-- claim-index:start -->
| ID | Contract declaration | Source theorem | Model / transition | Assumptions | Evidence | Explicit exclusions |
| --- | --- | --- | --- | --- | --- | --- |
| SC-KERNEL-DET | `kernel_transition_deterministic` | `KernelTransition.transition_deterministic` | `KernelTransition.transition` | Same state and command; two evaluations equal the named outcomes | Proved | Generated code and boot boundary |
| SC-SINGLE-CORE-BOOT-ADMISSION | `single_core_boot_admission_confined` | `BootTopology.accepted_implies_single_enabled_bsp`; `BootTopology.runtime_preserves_single_core_admission`; `BootTopology.runtime_cannot_publish_ap_start` | `BootTopology.decodeAndAdmitAuthoritativeAcpiTopology`, `BootTopology.admit`, and the finite `BootTopology.runtimeStep` vocabulary | Decoded old/new RSDP evidence selects the exact copied RSDT/XSDT address; every advertised root entry has one copied-table translation; the unique bounded, checksum-valid APIC table produces the exact accepted snapshot using one executing-APIC observation for BSP/executing identity; the runtime state carries the immutable admission premise and has not issued an AP start; `repository_authoritative_acpi_topology_admitted` supplies a concrete accepted witness | Proved | Copied-byte and physical-address fidelity, executing-APIC-ID sampling, dormant-AP and INIT/SIPI semantics, generated code, boot C/assembly, compiler/linker, QEMU, firmware, hardware, SMP/hotplug, and final-binary refinement |
| SC-DMA-QUARANTINE | `dma_quarantine_preserves_complete_projection` | `DMAQuarantine.unowned_device_preserves_complete_projection` | `DMAQuarantine.validate`, `DMAQuarantine.DeviceContract`, and the finite q35 snapshot/control model | Accepted snapshot for topology version `0x0001_0008_0002_0002`; named present unassigned function; explicit device contract that any device-originated mutation requires the named present function's PCI bus-master bit; `dma_quarantine_q35_nonvacuous` supplies the accepted nonempty witness | Proved | PCI enumeration/configuration semantics and completeness, firmware/QEMU/device obedience, hotplug, generated code, assembly/C/compiler, final-binary refinement, IOMMU and confidentiality/covert channels |
| SC-DMA-QUARANTINE-TRACE | `dma_quarantine_nonfatal_trace_preserves_complete_projection` | `DMAQuarantine.nonfatal_trace_preserves_quarantine_and_memory` | `DMAQuarantine.QuarantineTrace` over the issue-local runtime control and complete memory projection | Initial `RuntimeInvariant`; each trace step is either a continuing `runtimeGate` result or a named present function carrying the explicit `DeviceContract`; the accepted snapshot supplies unassigned ownership and disabled bus mastering; `dma_quarantine_q35_trace_nonvacuous` supplies a nonempty mixed q35 trace | Proved | The #104 global composite runtime and its subsystem transitions, PCI enumeration/configuration semantics, firmware/QEMU/device obedience, hotplug, generated code, assembly/C/compiler, final-binary refinement, IOMMU and confidentiality/covert channels |
| SC-DMA-QUARANTINE-GLOBAL | `dma_quarantine_global_runtime_preservation` | `dma_quarantine_global_runtime_nonvacuous`; `FailStop.bootRuntime_deferredBlockingRuntimeWellFormed`; `FailStop.DeferredBlockingRuntimeWellFormed.authoritative`; `FailStop.authoritativeGate_dmaAuthority`; `FailStop.authoritativeGate_neutralConstructors_preserve_dmaQuarantined`; `FailStop.runAuthoritativeOperations_preserves_dmaQuarantined` | `FailStop.AuthoritativeRuntimeWellFormed` and arbitrary finite `FailStop.runAuthoritativeOperations` suffixes over the sole authoritative `CompositeState` | `dma_quarantine_global_runtime_nonvacuous` derives the authoritative invariant for the accepted repository sample boot input; proof-carrying accepted finite q35 snapshot; ordinary, blocking, and deferred-drain operations structurally retain both PCI authority fields and carry no BDF, assignment, or Command-register payload; exact-projection compatibility independently gives complete global and DMA preservation for return selection, return completion, and restart | Proved | Complete #104 preservation still requires compatibility for the other unfinished constructors, but the DMA projection does not; PCI read freshness and semantics, firmware/QEMU/device obedience, hotplug, #105 canonical state codec/generated adapter, assembly/C/compiler, final-binary refinement, IOMMU and confidentiality/covert channels |
| SC-DMA-AUTHORITATIVE-PROJECTION | `dma_authoritative_unowned_device_preservation` | `FailStop.CompositeState.DMAQuarantined.unownedDevicePreservesCompleteProjection` | `DMAQuarantine.DeviceContract` over the sole authoritative `CompositeState` live PCI observation and complete memory projection | Authoritative runtime invariant; named present function; explicit contract that a device-originated mutation requires that function's observed PCI bus-master bit | Proved | PCI read freshness and semantics, firmware/QEMU/device obedience, hotplug, #105 canonical state codec/generated adapter, assembly/C/compiler, final-binary refinement, IOMMU and confidentiality/covert channels |
| SC-DMA-CONTROL-FAILSTOP | `dma_invalid_live_control_is_fatal_and_absorbing` | `FailStop.observeDMAControl_invalid_authoritative_suffix_absorbing` | Trusted live `FailStop.observeDMAControl` followed by `FailStop.runAuthoritativeOperations` | Running composite latch; validator rejection of the authoritative PCI observation | Proved | PCI read freshness and semantics, firmware/QEMU/device obedience, hotplug, generated adapter, assembly/C/compiler, final-binary refinement, reset behavior |
| SC-DMA-CONTROL-DRIFT-FAILSTOP | `dma_changed_live_control_is_fatal_and_absorbing` | `dma_changed_live_control_nonvacuous`; `FailStop.observeDMAControl_changed_authoritative_suffix_absorbing` | Trusted live `FailStop.observeDMAControl` followed by `FailStop.runAuthoritativeOperations` | Running composite latch; valid observation differing from the boot-accepted authoritative snapshot; `dma_changed_live_control_nonvacuous` supplies the accepted repository sample boot runtime and valid-but-distinct q35 Command-bit snapshot, then proves fatal absorption for every authoritative suffix | Proved | PCI read freshness and semantics, firmware/QEMU/device obedience, hotplug, generated adapter, assembly/C/compiler, final-binary refinement, reset behavior |
| SC-IOMMU-READ-CONFIDENTIALITY | `iommu_finite_read_confidentiality` | `IOMMU.read_confidentiality`; `IOMMU.actual_read_trace_confidentiality` | Finite successful `IOMMU.deviceRead` observations over the static assigned-device memory projection inside one coherent `AuthoritativeExtension` | `IOMMU.AuthoritativeExtension.Invariant`; each view carries the exact successful read result and assignment/mapping/frame lookup witnesses; equality is required only on each exact generation-, domain-, owner-, lifetime-, range-, and read-permission-authorized backing-frame view; `IOMMU.DeviceSemantics` separately trusts platform source/range fidelity and trusted software attachment of the active assignment generation before lookup, because PCIe does not carry that generation | Proved | VT-d programming/table walks, IOTLB state, PCIe requester-ID correctness, device obedience, generated code, assembly/C/compiler/linker, QEMU, firmware, final-binary refinement, timing/covert channels |
| SC-IOMMU-WRITE-INTEGRITY | `iommu_finite_write_integrity` | `IOMMU.isolated_trace_integrity`; `IOMMU.protected_trace_does_not_touch`; `IOMMU.unassigned_trace_does_not_touch`; `IOMMU.other_owner_trace_does_not_touch`; `IOMMU.write_integrity` | `IOMMU.runDeviceTrace` over authorized device writes and read observations inside one coherent `AuthoritativeExtension` | `IOMMU.AuthoritativeExtension.Invariant`; the target is a protected live frame, a physically unassigned live frame, or a live frame owned by someone other than the owner of every successful write in the trace; `validateCore` forbids two simultaneously live generations from sharing a `FrameId` | Proved | VT-d programming/table walks, IOTLB state and invalidation, PCIe requester-ID correctness, device obedience, concurrency/memory ordering, generated code, assembly/C/compiler/linker, QEMU, firmware, final-binary refinement |
| SC-IOMMU-NONFORGERY | `iommu_translation_nonforgery` | `IOMMU.translation_nonforgery`; `IOMMU.no_translation_without_live_assignment`; `IOMMU.Permission.attenuates_trans`; `IOMMU.consecutive_generations_never_reuse` | `IOMMU.translate` over finite assignments, domains, mappings, capabilities, exact object-to-frame bindings, and frame lifetimes inside one coherent `AuthoritativeExtension` | `IOMMU.AuthoritativeExtension.Invariant`; successful translation witness carries exact successful assignment, mapping, and frame lookup equations; each admitted capability resolves its object through `Core.frameAuthority` to the exact frame handle/lifetime; the platform supplies source/range faithfully and trusted software attaches the active assignment generation before lookup because PCIe does not carry it; owner/source/domain/frame are resolved from authoritative kernel state rather than public grant words | Proved | Hardware requester authentication, IOMMU programming and caches, generated codec/dispatcher, assembly/C/compiler/linker, QEMU, firmware, final-binary refinement |
| SC-IOMMU-CLEANUP | `iommu_teardown_cleanup` | `IOMMU.gated_teardown_removes_all_mappings`; `IOMMU.teardown_removes_all_mappings` | Invariant-bearing `IOMMU.gatedByKernel` assignment-teardown transition over the complete `AuthoritativeExtension` | `IOMMU.AuthoritativeExtension.Invariant`; accepted generation-checked assignment owned by the authoritative current owner; accepted result carries the coherent post-invariant | Proved | IOTLB invalidation and stale cached translations, hardware table updates, concurrency, generated code, assembly/C/compiler/linker, QEMU, firmware, final-binary refinement |
| SC-IOMMU-LIFETIME | `iommu_reachable_frame_release_denied` | `IOMMU.gated_release_rejects_reachable_frame`; `IOMMU.release_rejects_reachable_frame`; `IOMMU.retired_assignment_generation_rejects` | Invariant-bearing `IOMMU.gatedByKernel` frame release and static source/generation translation | `IOMMU.AuthoritativeExtension.Invariant`; exact live frame handle remains reachable from at least one active DMA mapping | Proved | IOTLB invalidation-before-reuse ordering, shared-page accounting, hardware caches, generated code, assembly/C/compiler/linker, QEMU, firmware, final-binary refinement |
| SC-IOMMU-FAILSTOP | `iommu_fatal_suffix_absorbing` | `IOMMU.halted_iommu_suffix_absorbing`; `IOMMU.gatedByKernel_preserves_authoritative_extension`; `IOMMU.kernel_operation_preserves_authoritative_extension` | Invariant-bearing coherent IOMMU authority extension of the sole #104 authoritative `FailStop.CompositeState` through `IOMMU.gatedByKernel` | Global execution latch already contains a typed fatal record; `IOMMU.AuthoritativeExtension.Invariant` supplies the complete cross-projection coherence law and is carried by every accepted control result | Proved | Partial machine mutations, reset/reboot behavior, VT-d and PCIe semantics, generated code, assembly/C/compiler/linker, QEMU, firmware, final-binary refinement |
| SC-DIRECT-PORT-USER-DENIAL | `direct_port_user_denial_preserves_devices` | `DirectPortIO.accepted_user_request_denied_gp`, `DirectPortIO.user_request_preserves_device_state` | `DirectPortIO.executeUser` over the finite TSS/IOPL control and complete device projection | Exact kernel-produced and freshly matched deny-all controls; user supplies only port, direction, width, and value; `DirectPortIO.policy_nonvacuous` supplies a concrete denial paired with an accepted changing kernel operation | Proved | RFLAGS/TSS decoding and loading, x86 I/O privilege and exception semantics, vector-13 normalization, generated code, assembly/C/compiler, devices, QEMU, hardware, final-binary refinement |
| SC-DIRECT-PORT-KERNEL-CONFINEMENT | `direct_port_kernel_operation_confined` | `DirectPortIO.kernel_acceptance_confined` | `DirectPortIO.executeKernel` and the finite serial/PIC/PIT/debug-exit manifest | Typed kernel acceptance; live controls authorize kernel privilege; exact stored/live controls; trusted dispatch supplies purpose separately from operation words | Proved | Call-site and final-ELF inventory, PCI configuration and MMIO, device semantics, generated code, assembly/C/compiler, QEMU, hardware, final-binary refinement |
| SC-DIRECT-PORT-CONTAINMENT | `direct_port_denial_survivor_contained` | `DirectPortContainment.denied_port_contained` | `DirectPortContainment.containDeniedPort` sequencing `DirectPortIO.executeUser` with `FaultDispatch.dispatch` over the authoritative scheduler/lifecycle/context/TLB state | Exact accepted, freshly matched deny-all controls; any successful atomic cleanup/dispatch (typed-reason `idle` or scheduler-derived `dispatch`); `DirectPortContainment.denied_port_contained_nonvacuous` supplies the concrete two-subject witness that denies the port and retires the faulting subject | Proved | RFLAGS/TSS decoding and loading, x86 I/O privilege and exception semantics, vector-13 normalization and vector-14 dispatch refinement, normalized-entry-to-model refinement, generated adapter, machine cleanup/restore and CR3 writes, devices, compiler/linker, QEMU, hardware, final-binary refinement |
| SC-KERNEL-WF | `kernel_transition_preserves_wellFormed` | `KernelTransition.transition_preserves_wellFormed` | `KernelTransition.transition` | `KernelTransition.WellFormed state` | Proved | Generated code and boot boundary |
| SC-CAP-AUTH | `capability_copy_no_authority_amplification` | `Capability.copy_no_authority_amplification` | `Capability.copy` | Full modeled pre-state; authority exists after copy | Proved | Information flow, concurrency, lifetimes, binary |
| SC-FRAME-OWNER | `frame_ownership_exclusive` | `FrameAllocator.ownership_exclusive` | Frame ownership state | Both ownership predicates hold for the same frame | Proved | Firmware truth and allocator integration |
| SC-FRAME-BUDGET-ISOLATION | `admitted_frame_budget_isolation` | `FrameBudget.available_allocation_accepted` | `FrameBudget.allocate` over fixed boot-admitted frame partitions | Live trusted subject; valid empty generation-bounded slot; fresh object identity; at least one free frame committed to that subject | Proved | Dynamic admission or reassignment, concurrency/SMP, fairness/timing, generated code, boot policy installation, QEMU, hardware |
| SC-PT-SEPARATION | `page_table_distinct_spaces_separated` | `X86PageTable.distinct_spaces_separated` | Encoded page tables and `walk` | Both leaves exist at the page and their frames differ | Proved | Hardware walks, CR3/TLB, compiler |
| SC-SYSCALL-CONFINEMENT | `syscall_authority_confinement` | `Syscall.dispatch_authority_provenance` | Trusted-context syscall dispatch | Post-dispatch modeled authority | Proved | Entry path establishing trusted context |
| SC-FAILSTOP | `failstop_halted_suffix_absorbing` | `FailStop.halted_suffix_absorbing` | `FailStop.runOperations` | Execution mode already equals the named halted record | Proved | Machine reset behavior, physical NMI delivery/blocking, and latch persistence |
| SC-COMPOSITE-AUTHORITATIVE-COMPATIBLE-GATE | `composite_authoritative_compatible_gate_contract` | `FailStop.authoritativeGate_deterministic`; `FailStop.authoritativeGate_completed_sound`; `FailStop.authoritativeGate_rejection_atomic`; `FailStop.authoritativeOperationCompatible_of_admissible`; `FailStop.authoritativeGate_preserves_authoritativeRuntimeWellFormed_of_admissible`; `FailStop.runAuthoritativeOperations_preserves_authoritativeRuntimeWellFormed_of_admissible`; `FailStop.runAuthoritativeRevokeAfterSend_preserves_authoritativeRuntimeWellFormed`; `FailStop.runAuthoritativeStaleResumableContextTrace_preserves_authoritativeRuntimeWellFormed`; `FailStop.authoritativeRevokeAfterSend_reachable_witness`; `FailStop.authoritativeStaleResumableContext_reachable_witness`; `FailStop.authoritative_halted_suffix_absorbing`; `composite_authoritative_admitted_trace_preserves_runtimeWellFormed`; `composite_authoritative_gate_rejection_reachable_witness` | Sole published `FailStop.authoritativeGate` and arbitrary finite `FailStop.runAuthoritativeOperations` traces over ordinary, authoritative blocking, and deferred-drain operations in the same `CompositeState` | `AuthoritativeRuntimeWellFormed` includes the complete blocking/deferred-cancellation classification and derives every operation-specific post-state compatibility obligation. Gate and arbitrary-trace preservation require only that invariant. A contained interrupt whose execution identity is not the authoritative lifecycle current identity is a typed atomic rejection; matching identity performs cleanup. Concrete executable witnesses cover an accepted sealed offer followed by accepted revocation of its independent send-only endpoint slot, and a boot-rooted well-formed retirement followed by an atomic `noDestination` resumable-context denial. Completed results expose the exact typed subsystem reply and successor, classified denials return the literal pre-state, fatal mode absorbs arbitrary mixed suffixes, and boot supplies the complete pre-invariant. | Proved | Generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-GATE-WF | `composite_gate_sealed_receive_preserves_runtimeWellFormed` | `FailStop.gate_sealed_receive_preserves_runtimeWellFormed` | Sealed-mailbox rejection path of `FailStop.gate` | Globally well-formed running composite state; the caller's canonical endpoint handle resolves to a mailbox with a pending sealed descendant | Proved | Other composite operations until their preservation slices land; generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-GATE-SEND-WF | `composite_gate_data_send_preserves_runtimeWellFormed` | `FailStop.gate_ipc_send_accepted_preserves_runtimeWellFormed` | Accepted data-send path of `FailStop.gate` | Globally well-formed running composite state and the exact typed `sent` subsystem result | Proved | Other accepted composite mutations until their preservation slices land; generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-GATE-RECEIVE-WF | `composite_gate_data_receive_preserves_runtimeWellFormed` | `FailStop.gate_ipc_receive_accepted_preserves_runtimeWellFormed` | Accepted data-only receive path of `FailStop.gate` | Globally well-formed running composite state and the exact typed provenance-bearing delivery result | Proved | Sealed capability installation uses the separate transfer-accept operation; other accepted composite mutations until their preservation slices land; generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-INFLIGHT-REVOCATION | `composite_subtree_revocation_reaches_in_flight_authority` | `FailStop.gate_capabilityRevokeSubtree_accepted_cancels_sealed_descendants`; `FailStop.gate_capabilityRevokeSubtree_accepted_authority_monotone`; `FailStop.gate_capabilityRevokeSubtree_accepted_retains_history`; `FailStop.gate_capabilityRevokeSubtree_accepted_preserves_unrelated`; `FailStop.gate_capabilityRevokeSubtree_accepted_preserves_runtimeWellFormed`; `CompositeDispatcher.inFlightRevocationCanonicalEdges_refine` | Accepted `capabilityRevokeSubtree` path of `FailStop.gate`, publishing through `CapabilityTransfer.publishSubtreeRevocation`; the concrete #175 trace `CompositeDispatcher.inFlightRevocationCanonicalEdges` over `FailStop.inFlightRevocationInitial` | Running composite state; the runtime-safe capability guard accepted the revocation and the lineage root resolved in the victim's selected slot; `inFlightRevocation_revocation_cancels_in_flight` and `inFlightRevocation_slot_reuse_distinct_generation` supply the concrete offer/revoke/empty-receipt/same-slot-replacement witness | Proved | Generation binding of the raw-slot revocation command words themselves; sender-termination, object-retirement, and endpoint-destruction cancellation triggers; concurrency/SMP; generated adapters, compiler, QEMU, hardware, and final-binary refinement |
| SC-COMPOSITE-BLOCKING-CONTEXT-WF | `composite_blocking_gate_preserves_blockingRuntimeWellFormed` | `composite_blocking_gate_preserves_contextWellFormed`; `FailStop.blockingGate_preserves_blockingRuntimeWellFormed`; `FailStop.blockingGate_preserves_wellFormed`; `FailStop.blockingGate_rejection_atomic`; `FailStop.blockingGate_completed_sound`; `FailStop.blockingGate_receive_delivered_preserves_blockingRuntimeWellFormed`; `FailStop.blockingGate_receive_idle_block_preserves_blockingRuntimeWellFormed`; `FailStop.blockingGate_receive_selected_block_preserves_blockingRuntimeWellFormed`; `FailStop.blockingGate_receive_blocked_preserves_blockingRuntimeWellFormed`; `FailStop.blockingGate_send_sent_preserves_runtimeWellFormed`; `FailStop.blockingGate_send_woke_preserves_blockingRuntimeWellFormed`; `FailStop.blockingGate_send_woke_context_valid`; `FailStop.blockingGate_cancel_cancelled_preserves_blockingRuntimeWellFormed`; `FailStop.blockingGate_cancel_cancelled_context_valid`; `FailStop.dispatchBlockingSend_woke_exact`; `FailStop.dispatchBlockingCancel_cancelled_exact`; `composite_blocking_gate_rejection_reachable_witness` | Every typed block, delivery, mailbox send, wake, cancellation, finite denial, and latch rejection through the execution-latched `FailStop.blockingGate` | Exact waiter/saved-context agreement and blocking scheduler coherence; one exhaustive theorem preserves the integrated global/runtime plus waiter/context invariant for every operation and typed result without a result-classification premise; every completed result fixes the running latch, exact typed dependency reply, and exact composite post-state; every typed successful block preserves the integrated invariant by total idle/selected case analysis; idle block clears the active translation, while immediate handoff selects the old queue head, consumes its kernel-owned resumable context, and switches the active translation to that same identity; a failed restore is a typed atomic rejection; completed delivery, wake, cancellation, and queue-only send use their exact success laws; wake/cancel publish only after finite validation inserts a context that is valid for the exact authoritative post-state; every outer-gate-classified nonfatal rejection returns the literal pre-state; the boot-produced empty waiter store supplies a concrete classified rejection | Proved | Retiring the legacy blocking entry point in favor of the authoritative successor; generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-CONTAINED-FAULT-CLEANUP | `composite_contained_fault_cleanup_and_deferred_trace_contract` | `composite_contained_fault_cleanup_preserves_context_boundary`; `composite_interrupt_then_deferred_trace_preserves`; `FailStop.authoritativeGate_interrupt_preserves_deferredBlockingRuntimeWellFormed`; `FailStop.runAuthoritativeInterruptThenDeferredDrains_preserves`; `FailStop.authoritativeGate_drainDeferred_preserves_deferredBlockingRuntimeWellFormed`; `FailStop.authoritativeGate_rejection_atomic`; `FailStop.interrupt_contained_cleans_faulting_subject` | Every inbound interrupt through `FailStop.authoritativeGate`, including contained user-fault cleanup or typed identity rejection, followed by any finite public deferred-cancel drain suffix | `DeferredBlockingRuntimeWellFormed` alone unconditionally preserves the invariant across every interrupt result and drain suffix. A contained action cleans up only when its execution identity equals authoritative `lifecycle.current`; mismatch is a typed atomic rejection. The stronger exact-cleanup subclaim assumes the matching identity and proves removal from lifecycle, scheduler, resumable, waiter, blocked-context, and deferred projections. | Proved | Machine-level context restoration, concurrency/SMP, generated adapters, compiler, QEMU, hardware |
| SC-COMPOSITE-BLOCKING-REJECTION-WF | `composite_blocking_gate_rejection_preserves_runtimeWellFormed` | `FailStop.blockingGate_rejection_preserves_runtimeWellFormed`; `FailStop.blockingGate_rejection_atomic`; `composite_blocking_gate_rejection_reachable_witness` | Every outer-gate-classified nonfatal rejection of `FailStop.blockingGate` | Globally well-formed composite state and the finite typed rejection classifier; literal state identity excludes projection drift on denial; the boot-produced empty waiter store supplies a concrete classified rejection | Proved | Global-invariant preservation for mutating block, wake, and cancellation results; generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-TRANSFER-OFFER-WF | `composite_gate_transferOffer_preserves_runtimeWellFormed` | `FailStop.transferOffer_operationPreservesRuntimeWellFormed`; `FailStop.gate_transferOffer_accepted_preserves_runtimeWellFormed` | Every canonical sealed-transfer offer through `FailStop.gate` | Globally well-formed composite state; endpoint/source identity is selected only by generation-bound words in the authoritative current caller's capability space | Proved | Termination-time pending-offer cancellation, generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-TRANSFER-ACCEPT-WF | `composite_gate_transferAccept_preserves_runtimeWellFormed` | `FailStop.transferAccept_operationPreservesRuntimeWellFormed`; `FailStop.gate_transferAccept_delivered_preserves_runtimeWellFormed` | Every canonical sealed-transfer receipt through `FailStop.gate` | Globally well-formed composite state; endpoint identity is generation-bound and the destination is a checked-empty slot of the authoritative current caller | Proved | Termination-time pending-offer cancellation, generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-GATE-CONTRACT | `composite_gate_typed_result_contract` | `FailStop.gate_completed_sound`; `FailStop.gate_mode_rejection_atomicity`; `FailStop.gate_subsystem_rejection_atomicity`; `FailStop.gate_classified_rejection_global_atomicity`; `FailStop.gate_classified_rejection_preserves_runtimeWellFormed`; `FailStop.gate_resumePreempt_rejected_atomic` | Every operation of `FailStop.gate` | Completed result, a busy/halted gate rejection, or the total public reply classifier identifying one of the finite typed nonfatal subsystem rejections, including every nonfatal resumable-preemption error; classified rejection requires no caller-supplied proof witness and preserves the literal state plus global invariant; `composite_subsystem_rejection_reachable_witness` supplies a concrete unknown-syscall non-vacuity witness | Proved | Operation-specific accepted-result predicates and invariant preservation for mutating subsystem successes; generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-AUTHORITY-CONFINEMENT | `composite_gate_authority_confinement` | `FailStop.authority_operations_result_sound` | Syscall, IPC, capability transfer, revocation, mapping, and unmapping operations through the running composite gate | Privileged subject and active address space are projected from the authoritative execution latch; public data contains only operation arguments | Proved | Entry/scheduler refinement, preemption and outgoing-return confinement (covered separately or pending), generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-CONTROL-WF | `composite_gate_control_preserves_runtimeWellFormed` | `FailStop.gate_selectUserReturn_preserves_runtimeWellFormed`; `FailStop.gate_restart_preserves_runtimeWellFormed` | Running return-authority selection and restart control operations | Globally well-formed composite state; running latch for return selection | Proved | Mutating subsystem operations until their preservation slices land; generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-MAPPING-WF | `composite_gate_mapping_preserves_runtimeWellFormed` | `FailStop.map_operationPreservesRuntimeWellFormed`; `FailStop.unmap_operationPreservesRuntimeWellFormed` | Raw kernel-confined map and unmap operations of `FailStop.gate` | Globally well-formed composite state; caller and active address space are projected from authoritative execution state | Proved | Syscall decoding (covered separately), page-table/TLB hardware, generated adapters, concurrency/SMP, compiler, QEMU |
| SC-COMPOSITE-SYSCALL-WF | `composite_gate_syscall_preserves_runtimeWellFormed` | `FailStop.syscall_operationPreservesRuntimeWellFormed` | Every fixed-width untrusted syscall tuple through `FailStop.gate` | Globally well-formed composite state; privileged caller and active address space are projected from authoritative execution state | Proved | Entry-path refinement, page-table/TLB hardware, generated adapters, concurrency/SMP, compiler, QEMU |
| SC-COMPOSITE-SCHEDULER-ADMISSION-WF | `composite_gate_schedulerAdmission_preserves_runtimeWellFormed` | `FailStop.scheduleAdd_operationPreservesRuntimeWellFormed`; `FailStop.scheduleAdd_missing_context_rejected_atomic` | Every subject identifier presented to the composite scheduler-admission operation | Globally well-formed composite state; a kernel-owned resumable context must already be staged for an accepted insertion | Proved | Context construction and machine restoration, other scheduler/preemption mutations, generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-TERMINATION-WF | `composite_gate_termination_preserves_runtimeWellFormed` | `composite_gate_termination_cleans_runtime_references`; `composite_gate_termination_defers_invalidated_peer`; `composite_terminateSubject_then_deferred_trace_preserves`; `composite_terminateCurrent_then_deferred_trace_preserves`; `FailStop.terminateSubject_operationPreservesRuntimeWellFormed`; `FailStop.terminateCurrent_operationPreservesRuntimeWellFormed`; `FailStop.gate_terminateSubject_accepted_preserves_runtimeWellFormed`; `FailStop.terminateSubject_accepted_cleans_runtime_references`; `FailStop.terminateSubject_accepted_defers_invalidated_waiter`; `FailStop.gate_terminateSubject_preserves_deferredBlockingRuntimeWellFormed`; `FailStop.authoritativeGate_terminateSubject_preserves_deferredBlockingRuntimeWellFormed`; `FailStop.runAuthoritativeTerminateSubjectThenDeferredDrains_preserves`; `FailStop.gate_terminateCurrent_accepted_cleans_deferred`; `FailStop.gate_terminateCurrent_preserves_deferredBlockingRuntimeWellFormed`; `FailStop.authoritativeGate_terminateCurrent_preserves_deferredBlockingRuntimeWellFormed`; `FailStop.runAuthoritativeTerminateCurrentThenDeferredDrains_preserves` | Every explicit subject identifier and the scheduler-selected current subject presented to the composite termination operations | Globally well-formed composite state; accepted cleanup is derived from the authoritative lifecycle/resumable state and atomically removes the dead identity from the ready queue, current selection, resumable bank, waiter index, blocked-context bank, deferred-cancellation bank, and every bounded in-flight sealed transfer; endpoint-owner termination also removes invalidated peer waiters, moves their exact saved contexts to typed deferred cancellation, and clears retired endpoint mailboxes; both termination spellings preserve the complete deferred invariant across any finite authoritative deferred-drain suffix without a reconstructed readiness witness, including explicit targets that begin blocked or already deferred | Proved | Machine destruction/restoration, generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-LEGACY-OPERATION-TRACE-WF | `composite_legacy_operation_mixed_trace_preserves_runtimeWellFormed` | `FailStop.runOperations_preserves_runtimeWellFormed_universally`; `FailStop.operation_preserves_runtimeWellFormed`; `FailStop.runtimeTraceOperation_complete` | Arbitrary finite interleavings of every constructor in the legacy `FailStop.Operation` surface: inbound interrupts, control, data IPC/sealed transfer, capability copy/revocation, mapping, subject lifecycle, and resumable-aware scheduling; authoritative blocking and deferred drains are excluded | Initial `RuntimeWellFormed`, which excludes the folded deferred-cancellation classification; contained faults reuse authoritative termination cleanup, fatal entry synchronizes both halt latches, and ordinary entry closes transient return/copy authority; revocation uses a fail-closed runtime-authority guard; raw dispatch/yield/tick requests reject atomically until they carry the kernel-owned save/restore payload; the scheduler-only legacy preemption constructor is retired in favor of authoritative `resumePreempt`; `composite_legacy_operation_mixed_trace_reachable_witness` runs a concrete sixteen-operation trace from the accepted repository boot plan | Proved | Authoritative blocking/deferred-drain preservation and the folded `AuthoritativeRuntimeWellFormed` invariant; generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-DIRECT-PORT-WF | `composite_gate_preserves_direct_port_boundary` | `FailStop.gate_directPortIO`; `FailStop.runOperations_directPortIO` | Every public composite operation and arbitrary finite mixed trace over `FailStop.gate` and `FailStop.runOperations` | Globally well-formed initial composite state, including accepted TSS/IOPL controls; public composite operations cannot invoke the separately typed kernel direct-port transition | Proved | Live TSS/RFLAGS/device reads, generated adapters, assembly/C/compiler, QEMU, hardware, final-binary refinement |
| SC-COMPOSITE-BOOT-WF | `composite_boot_runtime_wellFormed` | `FailStop.bootRuntime_runtimeWellFormed` | `BootPageTablePlan.compile`, then `FailStop.bootRuntime` before subject admission | The bounded boot page-table compiler returns the named plan; `composite_boot_runtime_reachable_witness` supplies the accepted repository sample input | Proved | Runtime subject/resource admission, generated adapters, boot-plan installation, compiler/linker, QEMU, firmware, hardware |
| SC-INTERRUPT-ENTRY-BINDING | `interrupt_entry_context_binding` | `InterruptEntry.makeNormalized_binds_context` | `InterruptEntry.makeNormalized` as the accepted-record constructor used by `normalize` | Reviewed six-entry ordinary manifest; explicit raw frame shape; kernel-owned entry context | Proved | x86 delivery, descriptor loads, assembly, generated C, compiler/linker, QEMU, hardware |
| SC-PAGE-FAULT-PROVENANCE | `page_fault_provenance_binding` | `InterruptEntry.normalizePageFault_accepted_binding`, `InterruptEntry.canonical_exact_bit_binding`, `InterruptEntry.decoded_canonical_page_fault_has_normalized_preimage`, `InterruptEntry.authorized_canonical_has_trusted_normalized_preimage`, `InterruptEntry.authorized_canonical_trusted_identities_representable`, `InterruptEntry.authorized_canonical_binds_trusted_context`, `page_fault_authorization_context_binding`, `InterruptEntry.rejected_canonical_authorizes_nothing`, `InterruptEntry.canonical_page_fault_authorization_preserves_state`, `InterruptEntry.unauthorized_canonical_cannot_mutate`, `InterruptEntry.pageFault_saved_gpr_confinement`, `InterruptEntry.pageFault_diagnostic_confinement`, `Oracle.page_fault_authority_mutations_attested` | `InterruptEntry.normalizePageFault` layered on an accepted generic vector-14 entry, the version-one 19-word canonical codec/action boundary with an independently supplied trusted context, and the executable authority attestation | Accepted `normalizePageFault` result; exact AMD64 `P`, `W/R`, `U/S`, `RSVD`, `I/D` profile; CR2 and paging controls sampled into kernel context; canonical frame/address; for action authorization, kernel-owned subject and address-space identities satisfy `InterruptEntry.AuthorityIdentityRepresentable` (`identity < 2^64`), so codec decode has an exact trusted subject/address-space `Nat` match plus exact CR3/control match, or explicit no-action rejection | Proved | Correct x86 error/CR2 delivery; assembly/compiler implementation of the policy-checked pre-call CR2 sample and labeled EFER read; generated C, linker, QEMU, firmware, hardware, final-binary refinement, demand paging and runtime mapping/TLB policy |
| SC-PAGE-FAULT-ACTIVE-SPACE-AGREEMENT | `page_fault_active_space_containment_agreement` | `FaultDispatch.dispatchPageFault_success_sound`, `FaultDispatch.pageFaultAgreement_sound`, `FaultDispatch.dispatchPageFault_integrity_fatal_atomicity`, `FaultDispatch.dispatchPageFault_fatal_atomicity`, `FaultDispatch.dispatchPageFault_rejected_unchanged`, `FaultDispatch.dispatchPageFault_preserves_wellFormed`, `FaultDispatch.page_fault_nonpresent_read_contained_nonvacuous`, `FaultDispatch.page_fault_supervisor_read_contained_nonvacuous`, `FaultDispatch.page_fault_readonly_write_contained_nonvacuous`, `FaultDispatch.page_fault_nx_execute_contained_nonvacuous`, `FaultDispatch.page_fault_validated_live_report_contained_nonvacuous`, `FaultDispatch.page_fault_reserved_live_table_is_fatal_nonvacuous`, `FaultDispatch.page_fault_out_of_range_live_table_is_fatal_nonvacuous`, `FaultDispatch.page_fault_malformed_ancestor_is_fatal_nonvacuous`, `FaultDispatch.page_fault_stale_lifecycle_mapping_is_fatal`, `FaultDispatch.page_fault_unissued_object_mapping_is_fatal`, `FaultDispatch.page_fault_reserved_error_is_fatal` | `FaultDispatch.dispatchPageFault` gating the existing `FaultDispatch.dispatch` cleanup over the canonical issue-#168 codec, proof-carrying `BootPageTablePlan.Plan`, a complete decoded live-root report, and authoritative resumable/TLB/virtual projections | Successful strengthened vector-14 outcome; kernel-selected current subject and active address space; independently authorized canonical snapshot; exact compiled root; report identity and full decoded-root validation; complete plan/virtual/lifecycle issued-object-and-frame agreement, including absence agreement; no matching cached translation; finite decoded live-walk/error denial agreement | Proved | x86 error/CR2/page-walk/TLB semantics, atomic sampling and decoding of the modeled live-root report from hardware, #104 publication of its already-owned compiled plan/report and global runtime projections, waiter/in-flight-transfer cleanup, generated adapter, assembly/C/compiler/linker, QEMU, firmware, hardware, final-binary refinement |
| SC-BOOT-IDT-PHASE | `boot_interrupt_phase_early_terminal` | `BootInterruptPhase.owned_bootstrap_event_terminal_absorbing` | `BootInterruptPhase.publish`/`dispatch`/`run` over the finite `inherited → bootstrap32 → bootstrap64 → runtime → terminal` publication chain | Kernel-owned `bootstrap32` or `bootstrap64` phase with no prior latch; `bootstrap_event_witnesses` supplies concrete vector-2 and error-code events in both bootstrap phases; `orderly_publication_witness` shows the reviewed publication chain reaches the runtime phase without a latch | Proved | Real descriptor loads and IDTR semantics, the reviewed pre-`lidt` entry prologue and one-instruction long-mode IDTR handoff window, x86/NMI delivery and frame construction, assembly stubs, generated C, compiler/linker, firmware, QEMU, hardware, final-binary refinement |
| SC-NMI-FAILSTOP | `nmi_terminal_failstop` | `FailStop.accepted_nmi_composite_atomicity` | Separate `InterruptEntry.terminalManifest`/`normalizeNmi` contract and `FailStop.gate` NMI step | Exact accepted vector-2, DPL0 interrupt gate on dedicated IST2; explicit five-word IST frame; matching running or handling snapshot; kernel-owned current subject/address space and trusted active-CR3 snapshot; frozen return-address-space view and page-table plan; `nmi_user_kernel_nonvacuous` supplies CPL3 and CPL0 witnesses | Proved | x86 NMI delivery, blocking/coalescing, descriptor/TSS loads, IST switching and stack mapping, raw snapshot construction, generated C, assembly/compiler/linker, QEMU, firmware, hardware, final-binary refinement, arbitrary instruction-boundary interleaving |
| SC-PRIVILEGE-ENTRY-STACK | `privilege_entry_stack_budget_sound` | `PrivilegeEntryStack.accepted_contract_conditions`, `PrivilegeEntryStack.accepted_budget_sound` | `PrivilegeEntryStack.authorize` over the guarded ordinary-entry layout and byte-budget model | Authorization returns an accepted budget for the supplied layout, reserved intervals, request, and composite state | Proved | Correspondence to linker symbols, live page tables, compiler stack-usage reports, call graph/disassembly, x86 entry and exception delivery, generated C, QEMU, hardware |
| SC-PRIVILEGE-ENTRY-CONTROL | `privilege_entry_control_single_mechanism` | `PrivilegeEntryControl.accepted_exactly_int80` | Finite selected-CPU, EFER, fast-entry MSR, boot write/read-back, extended-state, and inbound-manifest control model | Exact accepted kernel-produced/read-back control snapshot; completed extended-state denial predicate | Proved | CPUID/MSR reads and writes, reserved-bit masks, instruction/exception semantics and delivery, descriptors/entry assembly, generated C, compiler/linker, QEMU, hardware, final-binary refinement |
| SC-USER-RETURN-CONFINEMENT | `user_return_context_confinement` | `Interrupt.accepted_user_return_context_confined` | `Interrupt.validateUserReturn` | Exact accepted attestation; allowed purpose; running CPL3 frame/selectors; canonical and subject-contained RIP/RSP; complete raw/decoded flag policy; live, runnable, current subject; owned address space and matching CR3 | Proved | Frame/flag decoding, generated adapter, assembly, CR3/TLB writes, `iretq`, compiler, QEMU, hardware |
| SC-USER-RETURN-AUTHORITY | `user_return_authority_confinement` | `FailStop.accepted_user_return_has_bound_authority` | `BootPageTablePlan.compile`, `FailStop.selectReturnAuthority`, `FailStop.completeUserReturn` | Well-formed state and accepted outgoing return; selected subject/address-space view agrees with lifecycle ownership and exact compiled-plan root/user-text/user-stack leaves | Proved | Live installation of the compiled plan, generated C, assembly, compiler, QEMU, hardware |
| SC-USER-RETURN-LIVE-PLAN | `user_return_authority_requires_live_plan` | `FailStop.selectLiveReturnAuthority_armed_implies_live` | `FailStop.selectLiveReturnAuthority` | Armed composite authority implies the compiled user-text/user-stack leaf frames equal live memory-object bindings whose object kinds and allocator ownership agree with the active virtual-memory mappings; `user_return_composite_entry_witness` establishes syscall/return non-vacuity | Proved | Generated C, assembly, compiler, QEMU, page-walk hardware |
| SC-USER-RETURN-FAILSTOP | `user_return_rejection_failstop` | `FailStop.rejected_user_return_composite_atomicity` | `FailStop.gate` | Running composite state with armed authority and live plan/mapping refinement; authoritative normalized return request is rejected | Proved | Frame/context decoding before the modeled gate, generated C, assembly restore, terminal serial transport, compiler, QEMU, hardware |
| SC-EXTENDED-STATE-DENIAL | `extended_state_denial_confined` | `ExtendedState.denied_subject_confined` | `ExtendedState.classify` | Contained typed denial from the finite classifier; exact accepted CR0/CR4 denial snapshot; coherent bounded CPUID projection; normalized user origin and live kernel-owned subject/address-space binding | Proved | CPUID/control-register reads, instruction decoding and exception priority/delivery, entry assembly, generated C, compiler/linker, QEMU, hardware, final-binary refinement |
| SC-EXTENDED-STATE-CLEANUP | `extended_state_denial_cleanup_nonresumable` | `ExtendedState.denial_cleanup_cannot_resume` | `ExtendedState.dispatchDenied`, `ResumablePreemption.cleanupSubject`, `Scheduler.selectNext` | Typed contained denial; authoritative resumable-preemption lifecycle, ready queue, context bank, mapping and TLB projections | Proved | Normalized-entry-to-model refinement, generated adapter, machine cleanup/restore and CR3 writes, compiler/linker, QEMU, hardware |
| SC-EXTENDED-STATE-GLOBAL | `extended_state_global_runtime_preservation` | `ExtendedState.runComposite_preserves_policy` | `ExtendedState.compositeGate`, `FailStop.authoritativeGate` | Exact denied feature/control predicate before a finite sequence of authoritative interrupt, return, syscall, preemption, IPC, capability, mapping, lifecycle, or scheduler operations | Proved | Live CPUID/control-register reads, generated C, assembly, compiler/linker, QEMU, hardware, final-binary refinement |
| SC-FAULT-DISPATCH-NONRESUMPTION | `fault_dispatch_success_nonresumption` | `FaultDispatch.successful_nonresumption` | `FaultDispatch.dispatch` over the authoritative scheduler, lifecycle, resumable-context, virtual-mapping, and TLB state | The total composite result is successful (typed-reason `idle` or a scheduler-derived dispatch); the conclusion exposes that the pre-state current subject was live and runnable, then proves it dead and non-runnable, absent from scheduling/context state, and stripped of every pre-fault-owned address space and mapping | Proved | Normalized-entry-to-model refinement, waiter/in-flight-transfer cleanup, generated adapter, machine cleanup/restore and CR3 writes, compiler/linker, QEMU, hardware |
| SC-USER-FAULT-CLASS-CONTAINMENT | `user_fault_class_containment` | `FaultDispatch.success_reason_vector_binding`, `FaultDispatch.successful_nonresumption`, `FaultDispatch.kernel_origin_is_fatal` | `FaultDispatch.dispatch` over normalized `InterruptEntry` records for the manifest-bound page-fault, divide-error, and breakpoint classes | Successful composite result (`idle reason` or `dispatch reason context`) for an accepted CPL3 frame; the conclusion binds the typed reason to the manifest-decoded vector and its reviewed error-word convention, restates the complete non-resumption cleanup package, and makes every kernel-origin accepted frame from a running state the absorbing `kernelOrigin` halt | Proved | x86 #DE/#BP/#PF delivery, gate loads, and saved-RIP restart semantics (AMD64 manual assumptions), normalizer-to-machine refinement, generated adapter, machine cleanup/restore and CR3 writes, compiler/linker, QEMU, hardware, final-binary refinement |
| SC-USER-FAULT-SHARED-CONTAINMENT | `user_fault_shared_containment_vocabulary` | `UserFaultContainmentVocabulary.shared_contained_classes_one_transition` | `FaultDispatch.dispatch` and `DirectPortContainment.containDeniedPort` over one shared two-subject pre-state with real normalized `InterruptEntry` divide-error, breakpoint, and page-fault entries and the denied-port composition | Concrete shared witness (`DirectPortContainment.witnessSchedule`/`witnessSurvivorContext` and `UserFaultContainmentVocabulary.divideErrorEntry`/`breakpointEntry`); each real contained class reaches a survivor dispatch and the reason-independent post-state via `FaultDispatch.success_state_reason_independent` | Proved | x86 `#DE`/`#BP`/`#PF`/`#GP` delivery, gate loads, and saved-RIP restart semantics (AMD64 manual assumptions), normalizer-to-machine refinement, generated adapter, machine cleanup/restore and CR3 writes, compiler/linker, QEMU, hardware, final-binary refinement |
| SC-STALE-TRANSLATION-INVALIDATION | `stale_translation_invalidation_confined` | `StaleTranslation.unmap_accepted_effect`, `StaleTranslation.accepted_unmap_target_absent`, `StaleTranslation.unmap_wrong_owner_inert`; `CompositeDispatcher.mixed_unmap_effect_confined`, `CompositeDispatcher.mixed_accepted_unmap_publication_order`; runtime source/final-ELF ordering policy and QEMU CPL3 prefill/same-frame-reuse/denial/new-owner trace | `StaleTranslation.step` over the authoritative `LeanOS.TLB` cache/virtual-mapping state, the canonical composite dispatcher branch, and the bounded runtime page-7 mapping window | The acting subject, address space, and page are checked kernel state; an accepted unmap returns exact `Effect.page 2 7`, leaves the translation absent in the returned cache, and authorizes only the fixed B/page-7 PTE mutation followed by `invlpg` when active or B-root CR3 load when inactive before completion publication; a non-owner actor is a state-preserving rejection with no requested invalidation; in the dedicated image CPL3 B prefills the translation, the exact old physical frame is scrubbed, receives a replacement-lifetime canary, and is mapped present-user at A/page 7 only after unmap publication; B's reread without an intervening CR3 load takes a real not-present fault while the replacement canary remains intact, after which a fresh CPL3 A context reads that canary through A/page 7 while B/page 7 stays absent; the generated reused fixture binds fresh-object allocation and old-page absence, while bounded C owner/lifetime fields are scenario attestations rather than a general allocator | Proved/model-backed machine evidence | Processor instruction completion and page-walk/TLB coherence, compiler correctness beyond checked final-ELF order, bounded scenario metadata-to-general-runtime refinement, SMP shootdown, firmware/hardware, final-binary semantic refinement |
| SC-INVALIDATION-PUBLICATION-ORDER | `invalidation_publication_order` | `InvalidationPublication.prepare_retains_published`, `InvalidationPublication.prepare_preserves_wellFormed`, `InvalidationPublication.acknowledge_accepted_exact`, `InvalidationPublication.acknowledge_wrong_ticket_inert`, `InvalidationPublication.acknowledge_preserves_wellFormed`, `InvalidationPublication.release_not_published_before_ack`, `InvalidationPublication.reuse_publication_requires_retirement_ack`, `InvalidationPublication.publishReuse_preserves_wellFormed`, `InvalidationPublication.canonical_unmap_pending_retains_published`, `InvalidationPublication.canonical_unmap_publication_order`, `InvalidationPublication.canonical_switch_away_back_publication_order`, `InvalidationPublication.canonical_release_ack_cannot_splice_switch`, `InvalidationPublication.canonical_retirement_and_reuse_order`; `CompositeDispatcher.invalidationCanonicalEdges_refine`, `Oracle.hosted_invalidation_vectors_refine` | The standalone bounded stateful invalidation branch of `CompositeDispatcher.dispatch` | Accepted unmap, writable-to-read-only protect, release, destroy, and switch preparation retains the complete published pre-state and issues one fresh ticket plus exact page/space/flush effect; only the matching ticket/effect publishes the recorded successor; preparation, acknowledgement, and bounded reuse preserve the cache bound for both the visible state and any pending successor without trusting the supplied effect; the switch-away/back round trip keeps each root change unpublished until its own fresh flush ticket is acknowledged and rejects the first switch ticket at the return switch; wrong-owner/stale-handle transitions and malformed, wrong-effect, wrong-state, or stale-ticket acknowledgements are inert; bounded object-11 reuse requires acknowledged release and destruction with no pending effect | Proved | `bootRuntime` does not initialize publication from `resumable.translations`, and `InvalidationProjectionCoherent` is not folded into or preserved by `AuthoritativeRuntimeWellFormed`; therefore no boot-rooted/full-composite invalidation integration is claimed. The object-11 allocation is a finite witness, not #112's authoritative quota/reclamation path; generated C, scalar ABI/harness, compiler, machine invalidation completion, QEMU/hardware, final-binary refinement |
| SC-AUTHORITATIVE-RETIREMENT-FLUSH | `authoritative_retirement_and_root_switch_flush` | `ResumablePreemption.cleanupSubject_flushes_translations`, `ResumablePreemption.switch_accepted_flushes_translations`, `FailStop.gate_terminateSubject_accepted_flushes_translations`, `FailStop.gate_resumePreempt_accepted_flushes_translations`; `FrameBudgetScenario.retirement_effects_are_exact_and_trace_bound`, `FrameBudgetScenario.retirement_prepare_keeps_frame_unallocatable`, `FrameBudgetScenario.retirement_wrong_ack_stutters`, `FrameBudgetScenario.retirement_ack_publishes_reclaimed_frame`, `FrameBudgetScenario.invalidation_acknowledged_fresh_republication` | Accepted `FailStop.gate` subject termination and resumable preemption over the authoritative composite runtime, plus the bounded generated frame-budget retirement/publication path over the shared budget and scrub state | A globally well-formed running pre-state and an accepted lifecycle termination or accepted resumable save/select/restore; termination may release objects mapped through multiple roots; the frame-budget path requires the exact canonical transition token before released capacity or the reclaimed frame becomes published, and fresh mapping remains disabled until acknowledgement | Proved/model-backed machine evidence | Protection, retirement/reuse, and root-switch invalidation publication are not yet folded into the general runtime translation/memory projections; the frame-budget path is a bounded model and tested bridge rather than general runtime refinement; machine CR3 completion, compiler, QEMU/hardware, final-binary refinement |
| SC-SCHEDULED-ISOLATION | `scheduled_finite_trace_isolation` | `ScheduledObservation.finite_trace_lowEquiv` | Finite paired scheduled runs | Initial low-equivalence and equal declared public event projections | Proved | Timing, termination, public channels, binary refinement |
| SC-LIFETIME-IDENTITY-NO-REUSE | `lifetime_identity_no_reuse` | `BoundedLifecycle.bounded_identity_no_reuse` | `BoundedLifecycle.runOperations` over the bounded subject/object issuers composed with the reused subject-lifecycle, memory, endpoint, and address-space transitions | Pre-state counter/history agreement (`BoundedLifecycle.Invariant`); operations drawn from the composite creation/termination/release/destruction/IPC vocabulary; `lifetime_identity_no_reuse_nonvacuous` supplies the concrete invariant witness and mixed all-kinds trace | Proved | The #104 unified runtime and coherence between the subject-domain and object-domain capability stores, public create/destroy ABI exposure, machine-word refinement of the issuer counters, generated C, assembly/compiler/linker, QEMU, firmware, hardware, concurrency, reboot persistence |
<!-- claim-index:end -->

`SC-COMPOSITE-AUTHORITATIVE-COMPATIBLE-GATE` is the final published composite
runtime contract and derives every
`AuthoritativeOperationCompatible` post-state obligation from
`AuthoritativeRuntimeWellFormed`. Gate preservation and arbitrary finite trace
preservation have no operation-admission premise. Contained entry validates
the execution identity against authoritative lifecycle selection: a mismatch
is a typed, literal-state rejection and a match performs atomic cleanup.
A completed reply identifies either the
running execution latch or the exact out-of-band
`AuthoritativeOperation.ordinary (Operation.nmi raw context)` constructor. The
latter is the sole operation admitted while an ordinary handler is active; it
still fixes the exact typed reply and composite post-state and immediately
enters the absorbing terminal latch.

`FailStop.gate`, `FailStop.runOperations`, and `FailStop.blockingGate` remain
internal proof-migration surfaces for historical subsystem lemmas only. They
are not published runtime entry points and must not be used by adapters; the
only advertised composite transition and trace runner are
`FailStop.authoritativeGate` and `FailStop.runAuthoritativeOperations`.

`SC-FAULT-DISPATCH-NONRESUMPTION` concludes, rather than assumes, that the
faulting identity is the live, runnable kernel-owned current subject in the
pre-state. The successful result removes that identity from liveness, runnable,
current and ready scheduler state, the authoritative resumable bank, and every
address space and mapping that it owned before the fault. Supporting source
theorems separately establish totality, determinism, invariant preservation,
FIFO survivor safety, rejection atomicity, and typed fatal-cause preservation
with halt-latch absorption; the
stable claim does not silently promote those model properties into a
normalizer-to-machine or final-binary refinement statement. The exact cleanup,
progress, and trusted boundaries are documented in
[the fault-dispatch model](fault-dispatch.md).

`SC-USER-FAULT-CLASS-CONTAINMENT` extends the same composite transition to the
three typed contained classes. The reason value (page fault, divide error, or
breakpoint) is decoded only from the manifest-bound vector of the accepted
normalized frame; the claim binds it to the reviewed per-class error-word
convention and restates that all three classes share the identical cleanup and
survivor selection. The kernel-origin conjunct is a model statement about
accepted kernel-origin frames; kernel-origin #DE/#BP raw snapshots are already
terminal at the normalizer (`wrongOrigin`) under their user-only origin policy.
Vector/error-word/saved-RIP conventions come from the AMD64 manual and remain
trusted machine assumptions, not theorem conclusions.

`SC-PAGE-FAULT-ACTIVE-SPACE-AGREEMENT` strengthens only vector 14. A successful
`dispatchPageFault` result supplies the exact version-one decoded and
independently authorized snapshot, proves that the active address-space value
selects the matching compiled root and decoded live-root report, validates the
complete report against the plan, checks the CR2 page against the report and
current live mapping/lifetime projection, requires the exact matching TLB key
to be absent, and exposes the finite error-word/live-classifier denial
agreement. CR2 never selects a subject, address space, root, or survivor.
Malformed provenance, wrong root, invalid live report, stale mapping/lifetime,
cached-translation incoherence, integrity-signaling live-walk failures,
allowed accesses, and
error/walk mismatches latch fatal state without changing scheduler, context,
mapping, or translation stores. Kernel-origin faults have the same frozen-store
fatal result, while stale current/address-space scheduler bindings remain typed
state-preserving rejections before cleanup. The compiled plan is a read-only
argument already owned by the #104 global runtime; this slice adds no parallel
global state or invariant.

## Review workflow

To add a claim, add an independently typed wrapper theorem, then one unique row
with its assumptions and exclusions. To revise a claim, update its wrapper,
index row, and the relevant ADR/model document in the same change. Growing an
assumption or shrinking a conclusion requires an explicit rationale. Preserve
an old ID when its meaning is unchanged; otherwise deprecate it in prose and
introduce a new ID that names the superseded claim. Tested evidence must link a
repository-owned script and must never be classified as Proved.
