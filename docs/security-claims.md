# Security claim index

This index uses the evidence vocabulary fixed by
[ADR 0001](adr/0001-phase-1-scope-threat-model-and-tcb.md). Every **Proved** row
names an independently restated theorem in the default-built
`LeanOS.SecurityClaims` contract. Proofs apply to Lean models only; generated C,
the runtime, boot code, QEMU, hardware, timing, concurrency, and covert channels
remain excluded unless a separate refinement claim says otherwise.

<!-- claim-index:start -->
| ID | Contract declaration | Source theorem | Model / transition | Assumptions | Evidence | Explicit exclusions |
| --- | --- | --- | --- | --- | --- | --- |
| SC-KERNEL-DET | `kernel_transition_deterministic` | `KernelTransition.transition_deterministic` | `KernelTransition.transition` | Same state and command; two evaluations equal the named outcomes | Proved | Generated code and boot boundary |
| SC-DMA-QUARANTINE | `dma_quarantine_preserves_complete_projection` | `DMAQuarantine.unowned_device_preserves_complete_projection` | `DMAQuarantine.validate`, `DMAQuarantine.DeviceContract`, and the finite q35 snapshot/control model | Accepted snapshot for topology version `0x0008_0002_0002`; named present unassigned function; explicit device contract that any device-originated mutation requires the named present function's PCI bus-master bit; `dma_quarantine_q35_nonvacuous` supplies the accepted nonempty witness | Proved | PCI enumeration/configuration semantics and completeness, firmware/QEMU/device obedience, hotplug, generated code, assembly/C/compiler, final-binary refinement, IOMMU and confidentiality/covert channels |
| SC-KERNEL-WF | `kernel_transition_preserves_wellFormed` | `KernelTransition.transition_preserves_wellFormed` | `KernelTransition.transition` | `KernelTransition.WellFormed state` | Proved | Generated code and boot boundary |
| SC-CAP-AUTH | `capability_copy_no_authority_amplification` | `Capability.copy_no_authority_amplification` | `Capability.copy` | Full modeled pre-state; authority exists after copy | Proved | Information flow, concurrency, lifetimes, binary |
| SC-FRAME-OWNER | `frame_ownership_exclusive` | `FrameAllocator.ownership_exclusive` | Frame ownership state | Both ownership predicates hold for the same frame | Proved | Firmware truth and allocator integration |
| SC-FRAME-BUDGET-ISOLATION | `admitted_frame_budget_isolation` | `FrameBudget.available_allocation_accepted` | `FrameBudget.allocate` over fixed boot-admitted frame partitions | Live trusted subject; valid empty generation-bounded slot; fresh object identity; at least one free frame committed to that subject | Proved | Dynamic admission or reassignment, concurrency/SMP, fairness/timing, generated code, boot policy installation, QEMU, hardware |
| SC-PT-SEPARATION | `page_table_distinct_spaces_separated` | `X86PageTable.distinct_spaces_separated` | Encoded page tables and `walk` | Both leaves exist at the page and their frames differ | Proved | Hardware walks, CR3/TLB, compiler |
| SC-SYSCALL-CONFINEMENT | `syscall_authority_confinement` | `Syscall.dispatch_authority_provenance` | Trusted-context syscall dispatch | Post-dispatch modeled authority | Proved | Entry path establishing trusted context |
| SC-FAILSTOP | `failstop_halted_suffix_absorbing` | `FailStop.halted_suffix_absorbing` | `FailStop.runOperations` | Execution mode already equals the named halted record | Proved | Machine reset/NMI behavior and persistence |
| SC-COMPOSITE-GATE-WF | `composite_gate_sealed_receive_preserves_runtimeWellFormed` | `FailStop.gate_sealed_receive_preserves_runtimeWellFormed` | Sealed-mailbox rejection path of `FailStop.gate` | Globally well-formed running composite state; the caller's canonical endpoint handle resolves to a mailbox with a pending sealed descendant | Proved | Other composite operations until their preservation slices land; generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-GATE-SEND-WF | `composite_gate_data_send_preserves_runtimeWellFormed` | `FailStop.gate_ipc_send_accepted_preserves_runtimeWellFormed` | Accepted data-send path of `FailStop.gate` | Globally well-formed running composite state and the exact typed `sent` subsystem result | Proved | Other accepted composite mutations until their preservation slices land; generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-GATE-RECEIVE-WF | `composite_gate_data_receive_preserves_runtimeWellFormed` | `FailStop.gate_ipc_receive_accepted_preserves_runtimeWellFormed` | Accepted data-only receive path of `FailStop.gate` | Globally well-formed running composite state and the exact typed provenance-bearing delivery result | Proved | Sealed capability installation uses the separate transfer-accept operation; other accepted composite mutations until their preservation slices land; generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-BLOCKING-CONTEXT-WF | `composite_blocking_gate_preserves_contextWellFormed` | `FailStop.blockingGate_preserves_wellFormed`; `FailStop.blockingGate_rejection_atomic`; `FailStop.blockingGate_completed_sound`; `FailStop.blockingGate_receive_delivered_preserves_blockingRuntimeWellFormed`; `FailStop.blockingGate_send_sent_preserves_runtimeWellFormed`; `FailStop.blockingGate_send_woke_context_valid`; `FailStop.blockingGate_cancel_cancelled_context_valid`; `FailStop.dispatchBlockingSend_woke_exact`; `FailStop.dispatchBlockingCancel_cancelled_exact`; `composite_blocking_gate_rejection_reachable_witness` | Typed block, delivery, mailbox send, wake, and cancellation operations through the execution-latched `FailStop.blockingGate` | Exact waiter/saved-context agreement and blocking scheduler coherence; every completed result fixes the running latch, exact typed dependency reply, and exact composite post-state; completed delivery preserves the integrated global/runtime plus waiter/context invariant; a completed mailbox-only send preserves the global runtime invariant because no blocked receiver is released and the authoritative scheduler is unchanged; wake/cancel publish only after finite validation inserts a context that is valid for the exact authoritative post-state; every outer-gate-classified nonfatal rejection returns the literal pre-state; the boot-produced empty waiter store supplies a concrete classified rejection | Proved | Composition into `RuntimeWellFormed` for mutating block, wake, and cancellation results; immediate peer dispatch/CR3 restoration; generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-BLOCKING-REJECTION-WF | `composite_blocking_gate_rejection_preserves_runtimeWellFormed` | `FailStop.blockingGate_rejection_preserves_runtimeWellFormed`; `FailStop.blockingGate_rejection_atomic`; `composite_blocking_gate_rejection_reachable_witness` | Every outer-gate-classified nonfatal rejection of `FailStop.blockingGate` | Globally well-formed composite state and the finite typed rejection classifier; literal state identity excludes projection drift on denial; the boot-produced empty waiter store supplies a concrete classified rejection | Proved | Global-invariant preservation for mutating block, wake, and cancellation results; generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-TRANSFER-OFFER-WF | `composite_gate_transferOffer_preserves_runtimeWellFormed` | `FailStop.transferOffer_operationPreservesRuntimeWellFormed`; `FailStop.gate_transferOffer_accepted_preserves_runtimeWellFormed` | Every canonical sealed-transfer offer through `FailStop.gate` | Globally well-formed composite state; endpoint/source identity is selected only by generation-bound words in the authoritative current caller's capability space | Proved | Termination-time pending-offer cancellation, generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-TRANSFER-ACCEPT-WF | `composite_gate_transferAccept_preserves_runtimeWellFormed` | `FailStop.transferAccept_operationPreservesRuntimeWellFormed`; `FailStop.gate_transferAccept_delivered_preserves_runtimeWellFormed` | Every canonical sealed-transfer receipt through `FailStop.gate` | Globally well-formed composite state; endpoint identity is generation-bound and the destination is a checked-empty slot of the authoritative current caller | Proved | Termination-time pending-offer cancellation, generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-GATE-CONTRACT | `composite_gate_typed_result_contract` | `FailStop.gate_completed_sound`; `FailStop.gate_mode_rejection_atomicity`; `FailStop.gate_subsystem_rejection_atomicity`; `FailStop.gate_classified_rejection_global_atomicity`; `FailStop.gate_classified_rejection_preserves_runtimeWellFormed`; `FailStop.gate_resumePreempt_rejected_atomic` | Every operation of `FailStop.gate` | Completed result, a busy/halted gate rejection, or the total public reply classifier identifying one of the finite typed nonfatal subsystem rejections, including every nonfatal resumable-preemption error; classified rejection requires no caller-supplied proof witness and preserves the literal state plus global invariant; `composite_subsystem_rejection_reachable_witness` supplies a concrete unknown-syscall non-vacuity witness | Proved | Operation-specific accepted-result predicates and invariant preservation for mutating subsystem successes; generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-AUTHORITY-CONFINEMENT | `composite_gate_authority_confinement` | `FailStop.authority_operations_result_sound` | Syscall, IPC, capability transfer, revocation, mapping, and unmapping operations through the running composite gate | Privileged subject and active address space are projected from the authoritative execution latch; public data contains only operation arguments | Proved | Entry/scheduler refinement, preemption and outgoing-return confinement (covered separately or pending), generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-CONTROL-WF | `composite_gate_control_preserves_runtimeWellFormed` | `FailStop.gate_selectUserReturn_preserves_runtimeWellFormed`; `FailStop.gate_restart_preserves_runtimeWellFormed` | Running return-authority selection and restart control operations | Globally well-formed composite state; running latch for return selection | Proved | Mutating subsystem operations until their preservation slices land; generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-MAPPING-WF | `composite_gate_mapping_preserves_runtimeWellFormed` | `FailStop.map_operationPreservesRuntimeWellFormed`; `FailStop.unmap_operationPreservesRuntimeWellFormed` | Raw kernel-confined map and unmap operations of `FailStop.gate` | Globally well-formed composite state; caller and active address space are projected from authoritative execution state | Proved | Syscall decoding (covered separately), page-table/TLB hardware, generated adapters, concurrency/SMP, compiler, QEMU |
| SC-COMPOSITE-SYSCALL-WF | `composite_gate_syscall_preserves_runtimeWellFormed` | `FailStop.syscall_operationPreservesRuntimeWellFormed` | Every fixed-width untrusted syscall tuple through `FailStop.gate` | Globally well-formed composite state; privileged caller and active address space are projected from authoritative execution state | Proved | Entry-path refinement, page-table/TLB hardware, generated adapters, concurrency/SMP, compiler, QEMU |
| SC-COMPOSITE-SCHEDULER-ADMISSION-WF | `composite_gate_schedulerAdmission_preserves_runtimeWellFormed` | `FailStop.scheduleAdd_operationPreservesRuntimeWellFormed`; `FailStop.scheduleAdd_missing_context_rejected_atomic` | Every subject identifier presented to the composite scheduler-admission operation | Globally well-formed composite state; a kernel-owned resumable context must already be staged for an accepted insertion | Proved | Context construction and machine restoration, other scheduler/preemption mutations, generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-TERMINATION-WF | `composite_gate_termination_preserves_runtimeWellFormed` | `FailStop.terminateSubject_operationPreservesRuntimeWellFormed`; `FailStop.terminateCurrent_operationPreservesRuntimeWellFormed`; `FailStop.gate_terminateSubject_accepted_preserves_runtimeWellFormed`; `FailStop.terminateSubject_accepted_cleans_runtime_references` | Every explicit subject identifier and the scheduler-selected current subject presented to the composite termination operations | Globally well-formed composite state; accepted cleanup is derived from the authoritative lifecycle/resumable state and cancels every bounded in-flight sealed transfer | Proved | Machine destruction/restoration, generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-MIXED-TRACE-WF | `composite_universal_mixed_trace_preserves_runtimeWellFormed` | `FailStop.runOperations_preserves_runtimeWellFormed_universally`; `FailStop.operation_preserves_runtimeWellFormed`; `FailStop.runtimeTraceOperation_complete` | Arbitrary finite interleavings of every public composite operation: inbound interrupts, control, syscall, IPC/sealed transfer, capability copy/revocation, mapping, subject lifecycle, and resumable-aware scheduling | Globally well-formed initial composite state; contained faults reuse authoritative termination cleanup, fatal entry synchronizes both halt latches, and ordinary entry closes transient return/copy authority; revocation uses a fail-closed runtime-authority guard; raw dispatch/yield/tick requests reject atomically until they carry the kernel-owned save/restore payload; the scheduler-only legacy preemption constructor is retired in favor of authoritative `resumePreempt`; `composite_universal_mixed_trace_reachable_witness` runs a concrete sixteen-operation trace from the accepted repository boot plan | Proved | Generated adapters, concurrency/SMP, compiler, QEMU, hardware |
| SC-COMPOSITE-BOOT-WF | `composite_boot_runtime_wellFormed` | `FailStop.bootRuntime_runtimeWellFormed` | `BootPageTablePlan.compile`, then `FailStop.bootRuntime` before subject admission | The bounded boot page-table compiler returns the named plan; `composite_boot_runtime_reachable_witness` supplies the accepted repository sample input | Proved | Runtime subject/resource admission, generated adapters, boot-plan installation, compiler/linker, QEMU, firmware, hardware |
| SC-INTERRUPT-ENTRY-BINDING | `interrupt_entry_context_binding` | `InterruptEntry.makeNormalized_binds_context` | `InterruptEntry.makeNormalized` as the accepted-record constructor used by `normalize` | Reviewed three-entry manifest; explicit raw frame shape; kernel-owned entry context | Proved | x86 delivery, descriptor loads, assembly, generated C, compiler/linker, QEMU, hardware |
| SC-PRIVILEGE-ENTRY-STACK | `privilege_entry_stack_budget_sound` | `PrivilegeEntryStack.accepted_contract_conditions`, `PrivilegeEntryStack.accepted_budget_sound` | `PrivilegeEntryStack.authorize` over the guarded ordinary-entry layout and byte-budget model | Authorization returns an accepted budget for the supplied layout, reserved intervals, request, and composite state | Proved | Correspondence to linker symbols, live page tables, compiler stack-usage reports, call graph/disassembly, x86 entry and exception delivery, generated C, QEMU, hardware |
| SC-PRIVILEGE-ENTRY-CONTROL | `privilege_entry_control_single_mechanism` | `PrivilegeEntryControl.accepted_exactly_int80` | Finite selected-CPU, EFER, fast-entry MSR, boot write/read-back, extended-state, and inbound-manifest control model | Exact accepted kernel-produced/read-back control snapshot; completed extended-state denial predicate | Proved | CPUID/MSR reads and writes, reserved-bit masks, instruction/exception semantics and delivery, descriptors/entry assembly, generated C, compiler/linker, QEMU, hardware, final-binary refinement |
| SC-USER-RETURN-CONFINEMENT | `user_return_context_confinement` | `Interrupt.accepted_user_return_context_confined` | `Interrupt.validateUserReturn` | Exact accepted attestation; allowed purpose; running CPL3 frame/selectors; canonical and subject-contained RIP/RSP; complete raw/decoded flag policy; live, runnable, current subject; owned address space and matching CR3 | Proved | Frame/flag decoding, generated adapter, assembly, CR3/TLB writes, `iretq`, compiler, QEMU, hardware |
| SC-USER-RETURN-AUTHORITY | `user_return_authority_confinement` | `FailStop.accepted_user_return_has_bound_authority` | `BootPageTablePlan.compile`, `FailStop.selectReturnAuthority`, `FailStop.completeUserReturn` | Well-formed state and accepted outgoing return; selected subject/address-space view agrees with lifecycle ownership and exact compiled-plan root/user-text/user-stack leaves | Proved | Live installation of the compiled plan, generated C, assembly, compiler, QEMU, hardware |
| SC-USER-RETURN-LIVE-PLAN | `user_return_authority_requires_live_plan` | `FailStop.selectLiveReturnAuthority_armed_implies_live` | `FailStop.selectLiveReturnAuthority` | Armed composite authority implies the compiled user-text/user-stack leaf frames equal live memory-object bindings whose object kinds and allocator ownership agree with the active virtual-memory mappings; `user_return_composite_entry_witness` establishes syscall/return non-vacuity | Proved | Generated C, assembly, compiler, QEMU, page-walk hardware |
| SC-USER-RETURN-FAILSTOP | `user_return_rejection_failstop` | `FailStop.rejected_user_return_composite_atomicity` | `FailStop.gate` | Running composite state with armed authority and live plan/mapping refinement; authoritative normalized return request is rejected | Proved | Frame/context decoding before the modeled gate, generated C, assembly restore, terminal serial transport, compiler, QEMU, hardware |
| SC-EXTENDED-STATE-DENIAL | `extended_state_denial_confined` | `ExtendedState.denied_subject_confined` | `ExtendedState.classify` | Contained typed denial from the finite classifier; exact accepted CR0/CR4 denial snapshot; coherent bounded CPUID projection; normalized user origin and live kernel-owned subject/address-space binding | Proved | CPUID/control-register reads, instruction decoding and exception priority/delivery, entry assembly, generated C, compiler/linker, QEMU, hardware, final-binary refinement |
| SC-EXTENDED-STATE-CLEANUP | `extended_state_denial_cleanup_nonresumable` | `ExtendedState.denial_cleanup_cannot_resume` | `ExtendedState.dispatchDenied`, `ResumablePreemption.cleanupSubject`, `Scheduler.selectNext` | Typed contained denial; authoritative resumable-preemption lifecycle, ready queue, context bank, mapping and TLB projections | Proved | Normalized-entry-to-model refinement, generated adapter, machine cleanup/restore and CR3 writes, compiler/linker, QEMU, hardware |
| SC-EXTENDED-STATE-GLOBAL | `extended_state_global_runtime_preservation` | `ExtendedState.runComposite_preserves_policy` | `ExtendedState.compositeGate`, `FailStop.gate` | Exact denied feature/control predicate before a finite sequence of authoritative interrupt, return, syscall, preemption, IPC, capability, mapping, lifecycle, or scheduler operations | Proved | Live CPUID/control-register reads, generated C, assembly, compiler/linker, QEMU, hardware, final-binary refinement |
| SC-FAULT-DISPATCH-NONRESUMPTION | `fault_dispatch_success_nonresumption` | `FaultDispatch.successful_nonresumption` | `FaultDispatch.dispatch` over the authoritative scheduler, lifecycle, resumable-context, virtual-mapping, and TLB state | The total composite result is successful (`idle` or a scheduler-derived dispatch); the conclusion exposes that the pre-state current subject was live and runnable, then proves it dead and non-runnable, absent from scheduling/context state, and stripped of every pre-fault-owned address space and mapping | Proved | Normalized-entry-to-model refinement, waiter/in-flight-transfer cleanup, generated adapter, machine cleanup/restore and CR3 writes, compiler/linker, QEMU, hardware |
| SC-SCHEDULED-ISOLATION | `scheduled_finite_trace_isolation` | `ScheduledObservation.finite_trace_lowEquiv` | Finite paired scheduled runs | Initial low-equivalence and equal declared public event projections | Proved | Timing, termination, public channels, binary refinement |
<!-- claim-index:end -->

`SC-FAULT-DISPATCH-NONRESUMPTION` concludes, rather than assumes, that the
faulting identity is the live, runnable kernel-owned current subject in the
pre-state. The successful result removes that identity from liveness, runnable,
current and ready scheduler state, the authoritative resumable bank, and every
address space and mapping that it owned before the fault. Supporting source
theorems separately establish totality, determinism, invariant preservation,
FIFO survivor safety, rejection atomicity, and typed fatal-cause preservation
with halt-latch absorption; the
stable claim does not silently promote those model properties into a
normalizer-to-machine or final-binary refinement statement. The exact cleanup,
progress, and trusted boundaries are documented in
[the fault-dispatch model](fault-dispatch.md).

## Review workflow

To add a claim, add an independently typed wrapper theorem, then one unique row
with its assumptions and exclusions. To revise a claim, update its wrapper,
index row, and the relevant ADR/model document in the same change. Growing an
assumption or shrinking a conclusion requires an explicit rationale. Preserve
an old ID when its meaning is unchanged; otherwise deprecate it in prose and
introduce a new ID that names the superseded claim. Tested evidence must link a
repository-owned script and must never be classified as Proved.
