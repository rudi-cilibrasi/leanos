# Bounded Qotom bootstrap result queries

This hosted interface exposes the combined topology and bootstrap candidate. Its results do not grant platform or runtime admission.

- `out_of_range_is_zero` — Every query beyond the five defined result words returns zero for arbitrary supplied inputs.
